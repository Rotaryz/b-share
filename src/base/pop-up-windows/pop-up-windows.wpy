<template>
  <cover-view class="pop-up-windows" wx:if="{{showWindow}}">
    <cover-view class="window" wx:if="{{pop}}">
      <cover-view class="tip">{{content}}</cover-view>
      <cover-view class="pop-line"></cover-view>
      <cover-view class="btn">
        <cover-view class="btn-item" @tap="cancel">取消
          <cover-view class="btn-line"></cover-view>
        </cover-view>
        <cover-view class="btn-item" @tap="sure">确定</cover-view>
      </cover-view>
    </cover-view>
    <cover-view class="red-peck" wx:if="{{!pop}}">
      <cover-view class="up-header">
        <cover-view class="up-text">恭喜您！</cover-view>
        <cover-image class="red-image" wx:if="{{imageUrl}}"
                     src="{{imageUrl + '/defaults/share/page/pic-window_red@2x.png'}}"></cover-image>
      </cover-view>
      <cover-view class="money">
        <cover-view class="money-box">
          <cover-view class="money-icon">¥</cover-view>
          <cover-view class="mon-text" style="width:{{width}}px">{{money}}
          </cover-view>
          <cover-view class="money-icon monry-title">红包</cover-view>
        </cover-view>
      </cover-view>
      <cover-view class="red-text">红包已经存在你的钱包</cover-view>
      <cover-view class="red-btn" @tap="ansewr">回答认证问题，领取更多红包</cover-view>
    </cover-view>
    <cover-image class="red-close" wx:if="{{imageUrl && !pop}}"
                 src="{{imageUrl + '/defaults/share/page/icon-diary_del@2x.png'}}"
                 @tap="close"></cover-image>
  </cover-view>
</template>

<script>
  import wepy from 'wepy'
  import base from 'common/mixins/base'
  import URIS from 'common/js/config'
  import {ERR_OK} from 'api/base'

  export default class popUpWindows extends wepy.page {
    mixins = [base]

    config = {
      navigationBarTitleText: ''
    }

    data = {
      imageUrl: URIS.image,
      showWindow: false,
      content: '内容不会保存，退出编辑？',
      pop: false,
      money: 0,
      width: 0
    }

    infoRes(res) {
      if (res.error === ERR_OK) {
        return res.data
      }
    }

    methods = {
      cancel() {
        this.showWindow = false
        this.$emit('cancel')
      },
      sure() {
        this.showWindow = false
        this.$emit('sure')
      },
      show(content, pop, money) {
        typeof content === 'object' ? this.content = '' : this.content = content
        typeof pop === 'object' ? this.pop = true : this.pop = pop
        typeof money === 'object' ? this.money = 0 : this.money = money + ''
        this.width = this.money.length * 22
        this.showWindow = true
      },
      close() {
        this.showWindow = false
        this.$emit('close')
      },
      ansewr() {
        if (wepy.getStorageSync('user_info').role) {
          this.$parent.$navigate('/pages/name-authentication/name-authentication')
        } else {
          this.$parent.$navigate('/pages/answer-index/answer-index')
        }
      }
    }
  }
</script>

<style lang="stylus">
  @import '../../common/stylus/variable'
  @import '../../common/stylus/mixin'
  .pop-up-windows
    position: fixed
    height: 100vh
    width: 100vw
    background: $color-mask-bgc
    top: 0
    left: 0
    z-index: 9999
    .window
      background: $color-white
      all-center()
      width: 275px
      height: 165px
      border-radius: 4px
      .tip
        line-height: 124px
        font-size: $font-size-medium
        text-align: center
      .pop-line
        height: 1px
        background: $color-split-line
      .btn
        display: flex
        height: 40px
        .btn-item
          flex: 1
          height: 100%
          line-height: 40px
          text-align: center
          font-size: $font-size-medium-x
          color: $color-text-tr
          position: relative
          &:last-child
            color: $color-assist-pink
          .btn-line
            top: 0
            right: 0
            position: absolute
            height: 100%
            background: $color-split-line
            width: 1px

  .red-close
    row-center()
    height: 30px
    width: @height
    bottom: 15vw
    z-index: 500

  .red-peck
    background: $color-white
    all-center()
    height: 86.4vw
    width: 73.33vw
    border-radius: 4px
    .up-header
      border-top-right-radius: 4px
      border-top-left-radius: 4px
      background: #FFFAED
      height: 41.828%
      display: flex
      flex-direction: column
      align-items: center
      .red-image
        width: 57.82%
        height: 66.556%
      .up-text
        color: #F12A2A
        font-size: 5.6vw
        font-family: $font-family-meddle
        margin-top: 25px
    .money
      font-size: 39px
      color: $color-assist-pink
      line-height: 1
      height: 10.4vw
      margin-top: 16.5px
      position: relative
      .money-box
        all-center()
        .money-icon
          float: left
          font-size: $font-size-large-xx
          transform: translateY(50%)
        .monry-title
          float: left
          font-size: $font-size-small
          color: $color-text
          transform: translateY(23px)
          margin-left: 2px
        .mon-text
          width: 88px
          float: left
    .red-text
      font-size: $font-size-small
      color: $color-text-tr
      text-align: center
      margin-top: 12.5px
    .red-btn
      width: 88%
      height: 12vw
      line-height: 12vw
      text-align: center
      border: 1px solid $color-assist-pink
      border-radius: 50px
      color: $color-assist-pink
      margin: 27.5px auto
      font-size: $font-size-medium
</style>
